services:
    redis:
        image: redis:alpine
        command: [ redis-server, --maxmemory 128mb, --maxmemory-policy volatile-lru, --save "" ]

    db:
        image: mariadb:10.11
        working_dir: /application
        command: [ mysqld, --character-set-server=utf8mb4, --collation-server=utf8mb4_unicode_ci, --innodb-file-per-table=1 ]
        volumes:
            - opendxp-database:/var/lib/mysql
        environment:
            - MYSQL_ROOT_PASSWORD=ROOT
            - MYSQL_DATABASE=pimcore
            - MYSQL_USER=pimcore
            - MYSQL_PASSWORD=pimcore
        healthcheck:
            test: [ "CMD-SHELL", "mysql -h db -u$$MYSQL_USER -p$$MYSQL_PASSWORD $$MYSQL_DATABASE -e 'SHOW TABLES;'" ]
            interval: 10s
            retries: 6
            start_period: 1m
            timeout: 3s

    nginx:
        image: nginx:stable-alpine
        ports:
            - "80:80"
        volumes:
            - .:/var/www/html:ro
            - ./.docker/nginx.conf:/etc/nginx/conf.d/default.conf:ro
        depends_on:
            - php

    php:
        #user: '1000:1000' # set to your uid:gid
        image: opendxp/opendxp:php8.3-debug-latest
        environment:
            COMPOSER_HOME: /var/www/html
            PHP_IDE_CONFIG: serverName=localhost
            # Feed installer configuration via ENV variables.
            # See: https://github.com/open-dxp/opendxp/tree/1.x/doc/01_Getting_Started/02_Advanced_Installation_Topics
            OPENDXP_INSTALL_MYSQL_USERNAME: pimcore
            OPENDXP_INSTALL_MYSQL_PASSWORD: pimcore
            OPENDXP_INSTALL_MYSQL_PORT: 3306
            OPENDXP_INSTALL_MYSQL_HOST_SOCKET: db
            OPENDXP_INSTALL_MYSQL_DATABASE: pimcore
        depends_on:
            db:
                condition: service_healthy
        volumes:
            - .:/var/www/html

    supervisord:
        #user: '1000:1000' # set to your uid:gid
        image: opendxp/opendxp:php8.3-supervisord-latest
        depends_on:
            db:
                condition: service_healthy
        volumes:
            - .:/var/www/html
            - ./.docker/supervisord.conf:/etc/supervisor/conf.d/opendxp.conf:ro

    # The following two services are used for testing.
    # We restrict these services to the test profile only, so we don't spin them up with every `docker compose up`.
    # See: https://docs.docker.com/compose/profiles/
    test-db:
        profiles: [ 'test' ]
        extends: db
        volumes:
            - opendxp-test-database:/var/lib/mysql

    test-php:
        profiles: [ 'test' ]
        extends: php
        environment:
            APP_ENV: test
            OPENDXP_TEST_DB_DSN: ${OPENDXP_TEST_DB_DSN:-mysql://pimcore:pimcore@test-db/pimcore}
            OPENDXP_INSTALL_ADMIN_USERNAME: pimcore
            OPENDXP_INSTALL_ADMIN_PASSWORD: pimcore
            OPENDXP_INSTALL_MYSQL_HOST_SOCKET: test-db
        depends_on:
            test-db:
                condition: service_healthy
        volumes:
            - opendxp-test-var:/var/www/html/var
            - opendxp-test-public-var:/var/www/html/public/var

volumes:
    opendxp-database:
    opendxp-test-database:
    opendxp-test-var:
    opendxp-test-public-var:
